// main.cpp : Defines the entry point for the application.

#include <iostream>
#include <string>

static bool running;

int main()
{
    running = true;
    while (running)
    {
    }
    return 0;
}
